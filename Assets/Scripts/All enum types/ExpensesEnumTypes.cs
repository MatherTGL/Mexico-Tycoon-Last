namespace Expense
{
    public struct ExpensesEnumTypes
    {
        public enum ExpensesTypeObject : byte { Building, Transport }

        public enum AreaExpenditureType : byte { Production, Water, Security }
    }
}